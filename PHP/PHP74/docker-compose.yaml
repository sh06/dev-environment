version: '3.9'
services:
  php74:
    restart: always
    image: php74
    build:
      context: .
      dockerfile: Dockerfile
    container_name: php74
    volumes: 
        - ./../../../PHP:/var/www
        - ./conf/www.conf:/usr/local/etc/php-fpm.d/www.conf
        - ./conf/php-fpm.conf:/usr/local/etc/php-fpm.conf
    ports:
      - 9074:9000
    networks:
      develop:
        ipv4_address: 10.0.3.74
networks:
  develop:
    external: true